<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Playstation 5 Status</title>
        <meta name="description" content="Klaus Brage Galleri">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>

        <div class="container">
            
            <div class="list-group" id="list-group-playstation">
                <% for (var i = 0; i < stockArray.length ; i++) { %>

                <% if (stockArray[i].inStock == 0) { %>
                    <a href="<%= stockArray[i].productUrl %>" class="list-group-item list-group-item-action flex-column align-items-start" style="background-color:#EEC1C5; color:#632323;">
                        <div class="d-flex w-100 justify-content-between">
                            <h5 class="mb-1">Ikke på lager</h5>
                <% } else { %>
                    <a href="<%= stockArray[i].productUrl %>" class="list-group-item list-group-item-action flex-column align-items-start" style="background-color:#C0E1C5; color:#224F25;">
                        <div class="d-flex w-100 justify-content-between">
                            <h5 class="mb-1">På lager</h5>
                <% } %>

                            <small><%= stockArray[i].siteBrand %></small>
                        </div>
                        <p class="mb-1"><%= stockArray[i].siteName %></p>
                        <small>Pris <%= stockArray[i].price %>,-</small>
                    </a>
                <% } %>

                <a class="list-group-item list-group-item-action flex-column align-items-start text-muted">
                    <small  style="font-size:0.9rem; font-weight:light;">Sidst opdateret: </small>
                    <time class="updatedLast timeago" datetime="<%= stockArray[0].lastUpdated %>" style="font-size:0.9rem; font-weight:light;"></time>
                </a>
            </div>

        </div>

        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="js/jquery.timeago.js" type="text/javascript"></script>
        <script src="js/script.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    </body>
</html>